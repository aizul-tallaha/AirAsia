CREATE  TABLE `airasia`.`users` (
  `userName` VARCHAR(45) NOT NULL ,
  `userPwd` VARCHAR(45) NOT NULL ,
  `userEmail` VARCHAR(45) NOT NULL ,
  `userRole` VARCHAR(45) NOT NULL ,
  `userscol` VARCHAR(45) NOT NULL ,
  PRIMARY KEY (`userName`) );
